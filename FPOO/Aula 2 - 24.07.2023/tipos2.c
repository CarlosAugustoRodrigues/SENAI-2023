#include <stdio.h>

int	main(){
	int x; //Declarando variável. Variável = Espaço reservado na memória.
	x = 'A'; //Atribuindo valor a variável
	printf("Variavel x = %c",x); //%d e %i = Inteiro; %c = Caracter.
	
	return 0;
}
